syntax = "proto3";

package oracle;

message Request {
    uint32 n = 1;
    uint32 len = 2;
}

message Response {
    repeated uint32 nb = 1;
}

service BinaryOracle {
    rpc ToBinary(Request) returns (Response);
}