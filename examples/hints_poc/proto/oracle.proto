syntax = "proto3";

package oracle;

enum Size {
    SMALL = 0;
    MEDIUM = 1;
    LARGE = 2;
}

message Request {
    bool color = 1;
    Size size = 2;
}

message Response {
    bool color = 1;
    Size size = 2;
}

service SqrtOracle {
    rpc Sqrt(Request) returns (Response);
}